# AGENTS.md

## プロジェクト

Niora APIは、教科書の内容に対応したWorkspaceをk3s上に起動し、ブラウザから操作できる学習環境を提供するバックエンドAPIです。

## Context

作業に関係するドキュメントだけを確認してください。

- 要件と機能範囲：`docs/requirements.md`
- v0.0.1の対象範囲：`docs/adr/0001-define-version-0.0.1-scope.md`
- ドメイン用語：`docs/glossary/glossary.md`
- 現在の構成：`docs/architecture.md`
- API実装規約：`docs/api.md`
- 決定理由と履歴：`docs/adr/decisions.md`から関連するADR
- テスト方針：`docs/testing.md`
- 開発コマンド：`README.md`

すべてのADRを一律に読み込まず、変更対象に関係するADRだけを参照してください。

## ルール

- コード、API、データベース、ドキュメントでは用語集の英語名を使用する
- `architecture.md`を現在の構成の正とし、判断理由はADRを参照する
- 既存の要件やアーキテクチャに反する変更を暗黙に実装しない
- v0.0.1の対象外機能を、依頼なしに追加しない
- テストの作成と修正は`docs/testing.md`に従う
- APIの作成と修正は`docs/api.md`に従う
- ドキュメントは日本語、コード上の識別子は英語で記述する

### Pythonのimport

- パッケージ外から利用する公開クラス、関数、型は、責務を持つ最も近い`__init__.py`で再エクスポートし、`__all__`へ明示する
- 利用側は、公開名を個別モジュールではなく、その名前を公開するパッケージからimportする
- `__init__.py`内から同じパッケージのモジュールをimportするときは相対importを使用する
- レイヤー境界を越える再エクスポートや、循環importを発生させる上位パッケージへの集約は行わない
- コード例を含むドキュメントのimportも、コードと同じ公開パスへ揃える

## マルチエージェント運用

### 適用範囲

- この運用は、利用者の依頼を完了するためにコードの作成または変更が必要な場合にのみ適用する
- 質問への回答、説明、相談、設計、計画、調査、レビュー、またはドキュメントの確認だけを依頼された場合は、利用者が明示的に指定しない限りサブエージェントを起動しない
- コードの作成または変更が必要でも、小規模で明確な単一作業にはマルチエージェント運用を強制しない
- 複数ファイルにまたがる実装、設計判断を伴う変更、または独立した実装と検証が有効な場合にマルチエージェント運用を行う
- マルチエージェント運用を適用しない依頼では、利用者が指定したモデルだけで対応する

### Sol: Orchestrator

Solはメインエージェントとして全体を指揮し、最終判断と成果物に責任を持つ。

- タスクを、境界と完了条件が明確な単位へ分解する
- 要件、制約、関連ファイル、完了条件、期待する報告形式を各エージェントへ渡す
- LunaとTerraへの割り当て、作業順序、修正ループを管理する
- 要件、アーキテクチャ、実装方針に関する判断を行う
- Terraの指摘を採用、却下、保留に分類してからLunaへ修正を依頼する
- 最終差分とテスト結果を自ら確認し、完了を判断する
- 同じファイルを複数のエージェントに同時編集させない

### Luna: Implementer

Lunaは、Solから割り当てられた境界の明確な実装を担当する。

SolはLunaに対し、必要な情報を圧縮して次の内容を簡潔かつ明確に渡す。

- 実装する内容
- 変更してよい範囲
- 変更してはいけない範囲
- 関連する要件と既存規約
- 完了条件
- 実行するテスト

Lunaは次のルールに従う。

- 指定された範囲から作業を拡大しない
- 不明な要件や設計判断を推測で決めない
- 最小限の変更で実装する
- 実施した変更、テスト結果、未解決事項をSolへ報告する
- 判断が必要な場合はSolへ確認する

### Terra: Reviewer

Terraは、Lunaの実装を独立した立場でレビューする。

SolはTerraに対し、要件、完了条件、重要な確認項目、Lunaの実施内容を渡す。Terraは次の観点を確認する。

- 要件と完了条件を満たしているか
- 実行時エラーや挙動の後退がないか
- アーキテクチャとプロジェクト規約に反していないか
- セキュリティ境界を弱めていないか
- テストが不足していないか
- 不要な変更が含まれていないか

各指摘には、根拠、影響、対象ファイル、推奨修正を含める。判断が分かれる点や要件が不明な点は、自分で決定せずSolへ確認する。

Terraは原則としてコードを直接変更せず、指摘をSolへ返す。明白で局所的な修正のみ、Solが許可した場合に直接変更してよい。

### 修正ループ

1. Lunaが実装とテストを行う
2. Terraが要件に照らしてレビューする
3. Solが指摘の採否と修正方針を判断する
4. 採用した指摘をLunaが修正する
5. Terraが再レビューする
6. Solが最終検証を行い、完了を判断する

同じ問題で修正が繰り返される場合や、3回の修正後も完了条件を満たせない場合は、機械的に続行せずSolが原因と方針を再評価する。

## 禁止事項

- `.env`、`.env.local`、`.env.production`など、秘密情報を含む可能性がある環境変数ファイルを読み取らない
- 環境変数の名前と設定例は`.env.example`を参照する
- 必要な環境変数が`.env.example`に存在しない場合は、実際の`.env`を確認せず、`.env.example`への追加を提案する
- `env`、`printenv`、`set`など、実行環境の環境変数を一括出力するコマンドを実行しない
- パスワード、Token、秘密鍵、Cookie、認証Headerなどの秘密情報を、コード、ログ、例外、テストデータ、ドキュメントへ出力しない
- ADRで決定したセキュリティ境界、RBAC、NetworkPolicyを、明示的な判断なしに弱めない
- 作業範囲外のバグやセキュリティ問題を、通知せずに修正しない

## 通知ルール

次の問題を発見した場合は、作業を継続できる範囲で利用者へ通知してください。

- 秘密情報がコード、ログ、設定、テストデータへ含まれている
- 秘密情報が外部へ出力される可能性がある
- 認証、認可、RBAC、NetworkPolicyなどのセキュリティ境界に問題がある
- 実行時エラー、データ破損、リソース漏れなどにつながる可能性が高いバグがある
- 要件、`architecture.md`、ADR、実装の間に矛盾がある
- 必要なテストや検証を実行できなかった

通知には、可能な範囲で次の情報を含めてください。

- 問題のあるファイルまたは処理
- 問題が発生する条件
- 想定される影響
- 確認済みの問題か、未確認の可能性か

秘密情報を発見しても、その値自体は出力せず伏せてください。軽微なスタイル上の問題や、根拠のない推測は通知対象にしません。
