# AGENTS.md

## プロジェクト

Niora APIは、教科書の内容に対応したWorkspaceをk3s上に起動し、ブラウザから操作できる学習環境を提供するバックエンドAPIです。

## Context

作業に関係するドキュメントだけを確認してください。

- 要件と機能範囲：`docs/requirements.md`
- v0.0.1の対象範囲：`docs/adr/0001-define-version-0.0.1-scope.md`
- ドメイン用語：`docs/glossary/glossary.md`
- 現在の構成：`docs/architecture.md`
- 決定理由と履歴：`docs/adr/decisions.md`から関連するADR
- テスト方針：`docs/testing.md`
- 開発コマンド：`README.md`

すべてのADRを一律に読み込まず、変更対象に関係するADRだけを参照してください。

## ルール

- コード、API、データベース、ドキュメントでは用語集の英語名を使用する
- `architecture.md`を現在の構成の正とし、判断理由はADRを参照する
- 既存の要件やアーキテクチャに反する変更を暗黙に実装しない
- v0.0.1の対象外機能を、依頼なしに追加しない
- テストの作成と修正は`docs/testing.md`に従う
- ドキュメントは日本語、コード上の識別子は英語で記述する

## 禁止事項

- `.env`、`.env.local`、`.env.production`など、秘密情報を含む可能性がある環境変数ファイルを読み取らない
- 環境変数の名前と設定例は`.env.example`を参照する
- 必要な環境変数が`.env.example`に存在しない場合は、実際の`.env`を確認せず、`.env.example`への追加を提案する
- `env`、`printenv`、`set`など、実行環境の環境変数を一括出力するコマンドを実行しない
- パスワード、Token、秘密鍵、Cookie、認証Headerなどの秘密情報を、コード、ログ、例外、テストデータ、ドキュメントへ出力しない
- ADRで決定したセキュリティ境界、RBAC、NetworkPolicyを、明示的な判断なしに弱めない
- 作業範囲外のバグやセキュリティ問題を、通知せずに修正しない

## 通知ルール

次の問題を発見した場合は、作業を継続できる範囲で利用者へ通知してください。

- 秘密情報がコード、ログ、設定、テストデータへ含まれている
- 秘密情報が外部へ出力される可能性がある
- 認証、認可、RBAC、NetworkPolicyなどのセキュリティ境界に問題がある
- 実行時エラー、データ破損、リソース漏れなどにつながる可能性が高いバグがある
- 要件、`architecture.md`、ADR、実装の間に矛盾がある
- 必要なテストや検証を実行できなかった

通知には、可能な範囲で次の情報を含めてください。

- 問題のあるファイルまたは処理
- 問題が発生する条件
- 想定される影響
- 確認済みの問題か、未確認の可能性か

秘密情報を発見しても、その値自体は出力せず伏せてください。軽微なスタイル上の問題や、根拠のない推測は通知対象にしません。
